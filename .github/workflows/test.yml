name: Test

on:
  push:
    branches:
      - develop      
      - master
  pull_request:
    branches:
      - develop
      - master      

jobs:
  install-test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
      max-parallel: 4
    name: Python ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v4
      - name: setup-python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          architecture: "x64"
      - name: confirm pip version
        run: pip --version
      - name: installation
        run: pip install .[dev]
      - name: test
        run: python -m pytest tests/test_codecs.py --cov
  editable-install-test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.13"]
      max-parallel: 1
    name: Python ${{ matrix.python-version }} - editable install
    steps:
      - uses: actions/checkout@v4
      - name: setup-python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          architecture: "x64"
      - name: confirm pip version
        run: pip --version
      - name: installation
        run: pip install -e .[dev]
      - name: test
        run: python -m pytest tests/test_codecs.py --cov
